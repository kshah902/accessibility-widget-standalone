# Akby Accessibility Widget — Private Key for License Generation
# This is the ECDSA P-256 private key in JWK format.
# NEVER commit the actual .env file — only this example file.
#
# Generate a new key pair with:
#   node -e "const c=require('crypto');const{publicKey,privateKey}=c.generateKeyPairSync('ec',{namedCurve:'P-256'});console.log(JSON.stringify(privateKey.export({format:'jwk'})))"

A11Y_PRIVATE_KEY='{"kty":"EC","x":"...","y":"...","crv":"P-256","d":"..."}'
